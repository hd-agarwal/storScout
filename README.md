To build and run, 

In the project root folder, run

`gradle bootJar -x test`

Locate the the built jar in ./build/libs/ folder

Run on a terminal

`java -jar ./build/libs/storScout-0.0.1-SNAPSHOT.jar`
